<% flash.each do |name, msg| %>
<%= content_tag :div, {"data-alert" => "alert", :id => "flash_#{name}", :class => "alert-message #{name == :notice ? 'success' : name} fade in"} do |a| %>
  <%= link_to "x", "#", :class => "close" %>
  <p><%= msg %></p>
<% end %>
<% end %>
